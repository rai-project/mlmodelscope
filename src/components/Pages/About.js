import React from "react";
import { connect } from "cerebral/react";
import { Header } from "semantic-ui-react";

export default connect({}, function AboutPage() {
  return (
    <a href="https://rai-project.github.io/carml">
      <Header>TODO</Header>
    </a>
  );
});
